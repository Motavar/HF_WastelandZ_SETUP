# ============================================================
# HFWastelandZ — Admins / Player Roles
# Copy this file to:  $profile:HFWastelandZ_admins.conf
# ============================================================
#
# This file decides each player's ROLE (their permission tier),
# keyed by BOHEMIA IDENTITY GUID. The server is the only machine
# that reads it — players cannot fake their role.
#
# THE FIVE ROLES  (higher number = more power)
#   OWNER    40   you — full control, including server restart
#   ADMIN    30   full admin: Game Master (Zeus), promote others, save, etc.
#   MOD      20   moderator: kick, ban, mute, teleport, heal, god, announce
#   TRUSTED  10   donor / verified helper (reserved for future perks)
#   EVERYONE  0   default — every normal player (no line needed)
#
# FORMAT  (one player per line)
#   <ROLE> <IdentityGUID>
#
# The Identity GUID is a lowercase 8-4-4-4-12 code tied to a Bohemia
# account, e.g.  a1b2c3d4-5e6f-7a8b-9c0d-1e2f3a4b5c6d
# It is NOT a Steam64 number.
#
# HOW TO FIND A PLAYER'S GUID: have them join the server once, then
# read it from the server log — grep for  Authenticated player  and
# copy the  identityId=  value on the line with their name:
#     grep -rh "Authenticated player" <server>/logs/ | tail
#   (once the server is a systemd service:  journalctl -u wz-server1 ...)
#
# EXAMPLE
#   OWNER   a1b2c3d4-5e6f-7a8b-9c0d-1e2f3a4b5c6d
#   ADMIN   b2c3d4e5-6f7a-8b9c-0d1e-2f3a4b5c6d7e
#   MOD     c3d4e5f6-7a8b-9c0d-1e2f-3a4b5c6d7e8f
#   TRUSTED d4e5f6a7-8b9c-0d1e-2f3a-4b5c6d7e8f90
#
# ⚠ NO TRAILING SPACES / NO WINDOWS LINE-ENDINGS
#   The server matches admins by EXACT text. A stray trailing space
#   after the GUID, or Windows CRLF line-endings (from editing/pasting
#   on Windows), are invisible in most editors but make the ID silently
#   FAIL to match — the player stays EVERYONE with no error shown.
#   Edit this file on the server (nano) with no space after the GUID.
#   If admin isn't recognized, clean the file and verify:
#     sed -i 's/[[:space:]]*$//' HFWastelandZ_admins.conf
#     cat -A HFWastelandZ_admins.conf   # each role line must end in $ (no ^M, no space)
#   (A known parsing issue being fixed in the mod; the sed line is the fix for now.)
#
# ------------------------------------------------------------
# HOW TO SET SOMEONE'S ROLE
# ------------------------------------------------------------
# 1) FIRST admin (and the ONLY way to set OWNER): edit this file.
#    Add a line like  OWNER a1b2c3d4-...  then restart the server.
#
# 2) After that, do it live in-game (no restart) with the command:
#       /admin promote <name|playerId> <EVERYONE|TRUSTED|MOD|ADMIN>
#    - You must be ADMIN or OWNER to use it.
#    - An ADMIN can grant any role up to and including ADMIN. OWNER is
#      NEVER granted in-game (file-only, on purpose, so nobody can lock
#      you out) - set OWNER by editing this file.
#    - You can change anyone at or below your own rank (so an ADMIN can
#      demote a peer ADMIN down to MOD), but never someone ABOVE you.
#    - An OWNER can NEVER be changed in-game by anyone - edit this file.
#    - It rewrites this file for you, so the change survives restarts.
#
# 3) Or use the in-game F8 admin menu -> Players tab. The right-hand
#    STAFF list shows everyone in this file, including OFFLINE staff.
#    Click a player or staff row, then click a role button (twice to
#    confirm). Same rules and audit as /admin promote.
#
# SEE WHO HAS WHAT ROLE
#       /admin who        (lists everyone online with their role)
#
# WHAT EACH ROLE CAN DO
#   Each command has a minimum role. Defaults, for example:
#     MOD    — /admin who, kick, ban, mute, tp, heal, god, announce
#     ADMIN  — /admin promote, save, debug; all /mission and /spot editing
#     OWNER  — /admin restart
#   You can change any command's required role in
#   HFWastelandZ_command_tiers.conf (see that file).
#
# BEHAVIOR
#   No file / empty file  → no admins; Game Master access denied for all.
# ============================================================

# --- ROLES ---  (uncomment and use your own Identity GUIDs)
# OWNER a1b2c3d4-5e6f-7a8b-9c0d-1e2f3a4b5c6d
# ADMIN b2c3d4e5-6f7a-8b9c-0d1e-2f3a4b5c6d7e
# MOD   c3d4e5f6-7a8b-9c0d-1e2f-3a4b5c6d7e8f
